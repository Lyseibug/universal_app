import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App starts without errors', (WidgetTester tester) async {
    // Basic test placeholder - app requires initialized StorageService
    expect(true, isTrue);
  });
}
